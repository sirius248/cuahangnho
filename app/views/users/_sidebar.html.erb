	
		<% if !@user.avatar.exists? %>
			<%= image_tag('ninja.png', :size => '144x144', :class => "img-polaroid") %>
		<% else %>
			<%= image_tag @user.avatar.url, :size => '144x144', :class => "img-polaroid" %>
		<% end %>


		<br/>
		<% if signed_in? && current_user.id == @user.id %>

			<%= form_for @user, :html => { :multipart => true } do |form|%>
			  <%= form.file_field :avatar, :title=>"Chọn Avatar Khác", :class => 'btn btn-link btn-mini' %>
			  <br/>
			  
			  <%= form.button "Update", :class => 'btn btn-mini btn-info' %>
			<% end %>

		<% end %>

		<hr/>
		<h3><%=  @user.name %></h3>

		<p><b>Email:</b> <%= @user.email %></p>

		<% cache ['v3', 'setting'] do %>

		<a id="simple-menu" href="#sidr"><button class="btn">Setting</button></a>
 
		<div id="sidr">
		  <!-- Your content -->
		  <ul>
		    <li><a href="#">Security</a></li>
		    <li class="active"><a href="#">Messages</a></li>
		    <li><a href="#">About</a></li>
		    <li><%= link_to "Homepage", root_url %></li>
		  </ul>
		</div>

		<% end %>
